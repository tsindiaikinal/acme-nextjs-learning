const config = {
  trailingComma: 'es5',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  bracketSpacing: true,
  bracketSameLine: false, //by default
  arrowParens: 'always', //by default
  quoteProps: 'as-needed', //by default
  // printWidth: 80, //by default
  jsxSingleQuote: true,
  singleAttributePerLine: true,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
